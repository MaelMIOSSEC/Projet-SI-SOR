package server_data.entities;

/**
 * Represents the priority level of a task or issue in the system.
 * This enum can be used to categorize tasks based on their urgency or importance.
 */
public enum Priority {
    Low, Medium, Strong
}
