package server_data.entities;

/**
 * Represents the role of a user on a board in the system.
 * This enum is used to define the different levels of access and permissions that a user can have on a board.
 */
public enum Role {
    Member,
    Owner,
    Invited
}
